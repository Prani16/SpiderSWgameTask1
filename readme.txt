Prerequisite :

Download and install Node.js

1. Open command prompt and run below command (if npm not installed already )
	npm install -g http-server
2. Go to directories where Html files placed and Run below command
	http-server
3.copy the local URL 
4. open URL in web browser 
5. Click the grid to start the game
